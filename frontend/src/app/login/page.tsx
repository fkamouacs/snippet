import React from 'react';

import { UserAuthForm } from '@/components/user-auth-form';

const Login = () => {
  return (
    <div className=" w-full flex items-center justify-center lg:px-0">
      <UserAuthForm />
    </div>
  );
};

export default Login;
