{
  "name": "snippet",
  "version": "1.0.0",
  "description": "# Stack",
  "main": "index.js",
  "scripts": {
    "server": "tsx watch ./backend/src/index.ts",
    "client": "cd frontend && npm run dev",
    "build": "cd backend && rimraf dist && tsc",
    "ts.check": "cd backend && tsc --project tsconfig.json",
    "add-build": "cd backend && git add dist"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.44.1",
    "@testing-library/jest-dom": "^6.4.5",
    "@testing-library/react": "^15.0.7",
    "@types/jest": "^29.5.12",
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0",
    "pre-commit": "^1.2.2",
    "rimraf": "^5.0.7",
    "tsx": "^4.11.0"
  },
  "pre-commit": [
    "ts.check",
    "build",
    "add-build"
  ],
  "dependencies": {
    "ts-node": "^10.9.2"
  }
}
